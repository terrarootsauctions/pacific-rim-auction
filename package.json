{
  "name": "pacific-rim-auction",
  "version": "1.0.0",
  "description": "Pacific Rim Auction — Global Real Estate Auction Platform",
  "scripts": {
    "dev": "npx serve . -p 3000",
    "build": "echo 'Static site — no build step required'"
  },
  "dependencies": {
    "stripe": "^14.0.0"
  },
  "devDependencies": {
    "serve": "^14.0.0"
  }
}
